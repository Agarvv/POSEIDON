#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<worker.h>


#include<normalize.h>

int normalize(char* raw_req) {
    printf("%s Normalizer\n", raw_req); 
    fflush(stdout);
    return 0; 
}